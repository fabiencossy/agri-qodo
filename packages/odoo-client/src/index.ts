/**
 * @agri-qodo/odoo-client — client Odoo Enterprise.
 *
 * Wrappe XML-RPC (auth, lecture, écriture) et JSON-RPC (sale.order,
 * account.move, etc.). Implémentation réelle à l'étape 6 (M6).
 */

export const PACKAGE_NAME = "@agri-qodo/odoo-client" as const;
